# Commit

1. Review `git status` and `git diff HEAD`.
2. Stage only intended files.
3. Use one atomic commit with clear scope.
4. Prefer conventional tags: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`.
