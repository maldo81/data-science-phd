# Shared Command Blueprints

These files are tool-neutral command blueprints.

- `.claude/commands/*` and `.codex/commands/*` should mirror intent from this folder.
- Keep command behavior safe and checklist-oriented by default.
- Prefer guidance and verification steps over destructive automation.
