# Codex Workflow Notes

This folder contains Codex-oriented workflow guidance for repositories that install `agent-core`.

Start with:
- `.agents/codex/workflow.md`

Keep this folder reusable and project-agnostic.
