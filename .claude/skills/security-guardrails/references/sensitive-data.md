# Sensitive Data Baseline

- Treat PII/PHI/credentials as sensitive by default.
- Never commit secrets, tokens, or private keys.
- Keep secrets in runtime configuration, not source code.
- Avoid broad logging of sensitive payloads.
- Prefer de-identification when data must leave secure boundaries.
